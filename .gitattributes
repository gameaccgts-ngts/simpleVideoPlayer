# Keep the repo on LF so diffs stay clean across machines
* text=auto eol=lf

# Windows-native PowerShell script
*.ps1 text eol=crlf

# Binary assets — never normalize these
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.mp4  binary
*.webm binary
*.mov  binary
*.m4v  binary
